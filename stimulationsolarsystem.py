import pygame
import math
pygame.init() #used to initialise pygame
WIDTH, HEIGHT = 1000,1000
win=pygame.diaply.set_mode((WIDTH,HEIGHT)) #used to set the window size
pygame.display.set_caption("SOLAR SYSTEM STIMULATION")

